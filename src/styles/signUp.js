const style = (theme) => ({
  paper: {
    alignItems: "center",
    display: "flex",
    flexDirection: "column",
    paddingTop: theme.spacing(8),
  },
  form: {
    marginTop: theme.spacing(3),
    width: "100%",
  },
});

export default style;
