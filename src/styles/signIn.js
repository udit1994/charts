const style = (theme) => ({
  form: {
    marginTop: theme.spacing(1),
    width: "100%",
  },
  paper: {
    alignItems: "center",
    display: "flex",
    flexDirection: "column",
    paddingTop: theme.spacing(8),
  },
});

export default style;
