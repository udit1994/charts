const style = (theme) => ({
  root: {},
  paper: {
    padding: theme.spacing(10),
    textAlign: "center",
  },
  avatar: {
    backgroundImage: "url(https://source.unsplash.com/random)",
    textAlign: "center",
  },
});

export default style;
